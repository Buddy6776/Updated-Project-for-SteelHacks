import { requireAegisUser } from '../business-auth';
import PortalSidebar from '../portal-sidebar';

const safeguards=['Works only inside a folder the customer selects','Creates a patch preview before changing code','Requires the exact patch fingerprint as approval','Refuses dirty Git workspaces by default','Blocks secrets, credentials, and repository internals','Runs no arbitrary shell commands and has no remote-control channel'];

export const dynamic='force-dynamic';
export default async function ResolvePage(){const user=await requireAegisUser('/resolve');return <main className="portal-shell"><PortalSidebar active="resolve" displayName={user.displayName} email={user.email}/><section className="portal-main resolve-page"><header><span className="dash-kicker">PAID ADD-ON CONCEPT</span><h1>Aegis Resolve Pro</h1><p>A human-approved remediation assistant that can turn a confirmed finding into a reviewable code patch, then apply only the patch your team approves.</p></header><div className="resolve-hero"><div><span className="resolve-pill">Pilot architecture · Billing not connected</span><h2>From finding to a controlled fix</h2><p>The hosted assistant prepares a proposed patch. A downloadable local agent validates the workspace, displays the affected files and patch fingerprint, and changes code only after a person explicitly approves that exact patch.</p><div className="resolve-actions"><a href="/downloads/aegis-resolve-agent.py" download>Download local agent prototype ↓</a><a className="secondary" href="/downloads/aegis-resolve-agent-README.txt" download>Setup instructions</a></div></div><ol><li><span>01</span><div><strong>Confirm</strong><small>Validate the finding and select an authorized repository.</small></div></li><li><span>02</span><div><strong>Preview</strong><small>Review the proposed diff, affected files, and tests.</small></div></li><li><span>03</span><div><strong>Approve</strong><small>Authorize one exact patch fingerprint.</small></div></li><li><span>04</span><div><strong>Verify</strong><small>Run tests and rescan before deployment.</small></div></li></ol></div><section className="resolve-boundaries"><div><span className="dash-kicker">NON-NEGOTIABLE SAFEGUARDS</span><h2>Powerful without becoming a remote shell</h2></div><ul>{safeguards.map(item=><li key={item}>✓ {item}</li>)}</ul></section><div className="resolve-limit"><strong>What this prototype does not do</strong><p>It does not generate AI patches, connect billing, deploy to production, edit server configuration, or fix every finding automatically. A production version needs an authenticated AI service, organization roles, isolated test environments, signed agent releases, audit logs, rollback controls, and independent security review.</p></div></section></main>}
